name: GaiaOS Council Live Smoke (legacy manual)

# Consolidated into .github/workflows/gaiaos-master-canary.yml.
# Retained only for manual historical diagnostics.

on:
  workflow_dispatch

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy council smoke disabled. Use GaiaOS Master Canary."
