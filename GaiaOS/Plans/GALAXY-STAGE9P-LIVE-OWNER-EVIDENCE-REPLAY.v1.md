# GALAXY Stage 9P · Source-rebound owner evidence, zero fresh inference

AUTHORITY: NAOMI / LIGEIA. IMPLEMENTATION: 58 · ANVIL. STATUS: SOURCE IMPLEMENTATION, CI AND DEPLOYMENT SEPARATE. BIGBANG LOCKED.

## The real problem found after Stage 9O

Stage 9O counts existing HMAC-attested owner/model judgments but the original 2026-09-25 Stage 9F model receipt is **unsigned** and predates the sample fingerprint. A retrospective signature would misrepresent its provenance. We must not trigger another paid or provider call without a fresh, specific owner consent, and we must not say Stage 9O is live just because GitHub CI passed.

Unlike that model output, two **existing signed redacted owner-only artifacts** do exist under `GaiaOS/Proof/`:
- Stage 9I owner oracle: cases 0=B, 1=A, 2=COLLISION, under one keyed fingerprint.
- Stage 9I COLLISION: owner-authorized two-source exact-quote readback and unchanged legacy parity for that same fingerprint. Both receipts are dated owner-relayed evidence, not independent third-party observation.

Stage 9P verifies those signatures with the configured private API key, then freshly re-reads today's eligible approved two-current/five-case MemoryOS sample using the existing *model-free* private owner preview. It requires the exact current sample HMAC to match the old signed fingerprint, with HEATDEATH locked before and after.

The actual private preview is never sent to the browser, copied to GitHub or included in the redacted report. No retrospective signature, owner-label overwrite, relationship promotion or historical memory is created.

## Normal entry point and one-action proof

Integrate as an OPTIONAL, bounded nested `owner_evidence_replay` item beneath the existing `current_literal_readback` result of the *same* authenticated `POST /gaiaos/memory/one-click-readiness`. Call only after the existing remote-storage and two-source literal/HEATDEATH checks are PASS, using the owner API key already verified at the endpoint. The browser strictly rebuilds allowlisted finite fields before displaying or copying. No new page, token, owner action or seventh top-level check.

Use one owner click after deploying the current main: `/gaiaos/memory/one-click-console`, then return the existing redacted report. The live running commit reported by `/health` must match the merged Stage 9P source coordinate before claiming deployed adoption. Normal output is an owner-relayed observation; CI only proves tests against synthetic fixtures.

## Two evidence classes, no false promotion

A Stage 9P PASS means the signed **archived** owner's judgments and **archived** dual-source exact literal readback remain cryptographically bound to the present approved two-record sample. Current literal readback/legacy parity are separately checked by the pre-existing same-click check. Signed model receipts are **not** present in the canonical proof directory, and Stage 9O's existing model comparison still requires a genuinely signed separate model receipt.

A Stage 9P HOLD is explicitly possible when the key rotated, archived receipt changed, protected release state moved, the sample changed or the current source read is unavailable. Do not weaken sample admission, infer an absent historical transition or fabricate a replacement receipt to make it pass. Historical supersession, independent semantic entailment, representative/general semantic quality, comprehensive restart/storage proof and owner-authorized BIGBANG activation remain separate HOLDs.

## Engineering and safety

- `api/gaiaos_owner_evidence_replay.py`: bounded JSON receipt reading; Stage 9H HMAC validation; private source sample rebind; explicit failure categories.
- `api/gaiaos_one_click_readiness.py` and `api/gaiaos_one_click_console.py`: existing one-action integration and strict redaction. The parent six-check status/release decision is not upgraded.
- `api/gaiaos_app.py`: hand the already-authenticated API key to the read-only subcheck.
- `api/Dockerfile`: copy the new module into the normal deployed image alongside the already copied GaiaOS Proof source.
- Stage 7 PR gate compiles and runs isolated tests, Stage 9L existing actual-served browser contract, Stage 6, normal Docker import and HEATDEATH independent image build. Run CI before merging and require actual owner-controlled manual Render deployment afterward.

Permanent `//PW:PRESERVE//`, exactly six separate member E-LANES and the original native HEATDEATH fallback are untouched. No model call, writes, schedule, database relation, global weighting, deployment or BIGBANG activation is authorized by source merge.
