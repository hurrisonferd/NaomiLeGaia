# Stage 9I · Two-source COLLISION read-only proof

**Authority:** NAOMI/LIGEIA. **Engineering:** 58 · ANVIL. **Activation:** NONE. **Status at source merge:** source CI only; production deploy needs separate owner action.

## Purpose

The first live Stage 9F test produced three source-grounded, strict GALAXY readbacks but failed its generator-assigned unique-target oracle. Naomi independently judged B, A, and COLLISION (both A and B). Stage 9G bound future owner/model receipts to one keyed sample. Stage 9H cryptographically attests strict redacted owner and model receipts and compares categories. None of these alone independently checks the two separate real-source retrievals behind an owner COLLISION decision.

Stage 9I adds that narrow proof. It **does not** claim that two retrieved records genuinely answer the natural-language question, that an independent evaluator has certified semantic entailment, or that historical release evidence exists.

## One bounded optional owner action

From the existing authenticated technical console, finalize a Stage 9H owner-oracle receipt for the current fingerprinted five-case sample. If any owner-adjudicated case is COLLISION, the browser presents two separate private input boxes. The owner selects one exact, meaningful contiguous excerpt from Statement A and one from Statement B. No provider/model inference or fresh authorization for the previously consumed Stage 9F model call occurs.

The owner-only `POST /gaiaos/memory/augury-collision-two-source-shadow` route requires an authenticated, HMAC-attested Stage 9H owner receipt whose selected case is COLLISION with support set `[0,1]`. It independently reruns the bounded technical sample preparation and the authenticated two-record owner preview; the exact Stage 9G HMAC fingerprint, two current approved record IDs, source statements, source provenance, status, authority, scope and governing eligibility must still agree. The two private excerpts must match their corresponding approved source statement **byte for byte** and each be 12–240 characters long.

Each quote separately passes the **unchanged** strict Phase3J two-rare-concept source-first compiler. The existing operational GALAXY validator must accept each resulting read-only packet and that packet must contain the corresponding exact record ID. The carrier obtains native HEATDEATH retrieval before and after and requires byte-equivalent legacy output and an unchanged release-locked control state. This is strictly bounded to two records, five staged questions, two quote compilations, two strict readbacks and the existing limit-100 MemoryOS population scan. Any missing approval, absent provenance, old unsigned owner receipt, changed sample, malformed/foreign quote, inadequate literal concepts, missing GALAXY record, parity regression, or released BIGBANG control returns HOLD.

A successful source-membership-only result is `PASS_TWO_SOURCE_READBACK_ONLY`, with exact-quote, strict literal compile, and verified GALAXY readback flags for A and B, plus legacy parity. That result may be HMAC-attested as a new strict redacted `collision` receipt with the same owner key and fingerprint. No raw statement, query, source ID, excerpt or key leaves the authenticated browser or appears in the copied proof. Early HOLDs cannot be signed.

## Boundary and continuation

`collision_entailment_independently_proven=false`, `general_semantic_quality_proven=false`, `historical_coverage=false` and `release_activated=false` remain explicit even on a successful dual-source readback. The new signed collision proof is independent evidence of source retrievability only. Any future integration with Stage 9H's owner/model comparison must verify its attestation, fingerprint and case, while continuing to distinguish a model selecting one valid record from a model correctly reporting COLLISION. Historical supersession, Turso/restart recovery, global retrieval weighting and the final owner release remain independent locked gates.

Source CI tests use only a synthetic two-record store and a mocked strict compiler and GALAXY reader to test the sequencing, validation and denial paths; they do not prove real-store semantic quality. The Stage 7 PR gate includes the Stage 9I route tests, the permanent actual-served JavaScript/button check, and both the normal and independent HEATDEATH image builds. Render autoDeploy stays OFF, and no second model call or production deployment is authorized by this stage.
