# Stage 9: approved technical-memory preflight

Owner: NAOMI. Source-only until independently verified on the live carrier. Read-only. BIGBANG LOCKED.

This additive preflight reads at most 100 existing MemoryOS records and 100 explicit verified SUPERSEDES edges, restricted internally to non-fixture, Naomi-authorized technical records about PRESERVE, independent E-LANES, HEATDEATH, or GALAXY. No synthetic seeds, no new memory candidates, no cross-scope data, no inference of SUPERSEDES from REVISES. A missing real superseded record returns HOLD. Exactly three current queries spanning two records, one explicitly superseded historical query with a distinguishing old-version marker, and two adversarial negatives are prepared only when sufficient evidence exists.

Browser convenience: GET /gaiaos/memory/technical-preflight returns only redacted topic names, aggregate readiness and hold reasons. The homepage freely issues browser session cookies; these must NOT be represented as owner authentication. Nothing private leaves this GET. The explicit POST /gaiaos/memory/technical-review REQUIRES the existing private bearer authorization and returns only redacted case statuses. It runs the original Stage 7 read-only verifier; no raw statements, IDs, queries, token, DB URL, or memory details leave the endpoint. Caller must not paste API keys into ChatGPT or GitHub Actions logs. The read-only POST must never be linked to a public browser cookie, because that cookie can be acquired by anyone visiting GaiaOS.

The preview can HOLD without any change. A sample PASS is not BIGBANG release, representative quality, a full backup, live deployment proof, nor permission for production activation. Independent HEATDEATH recovery, authenticated real-store full review and explicit Naomi cutover authorization remain separate. `//PW:PRESERVE//`, all six separate E-LANES, and existing owner write guards are unchanged.

No scheduled workflows or canaries are added. Existing PR-only Stage 7 gate tests and normal/recovery image builds run on the PR, and no GitHub test should read real memory.
