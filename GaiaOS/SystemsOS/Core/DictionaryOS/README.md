# DictionaryOS — GaiaOS Semantic Entrance

```text
AUTHORITY: NAOMI
OWNER: DictionaryOS
STATUS: ACTIVE GAIA-NATIVE SOURCE / READ-ONLY QUERY
CLASS: CORE NAVIGATION SUPPORT
```

DictionaryOS turns Naomi-natural language into bounded GaiaOS object candidates.

```text
NATURAL PHRASE
→ canonical term candidates
→ aliases / collisions
→ owner + source references
→ stable object IDs
→ YggdrasilOS relationship traversal when material
```

DictionaryOS names and resolves. It does not create authority, settle operator identity, execute effects, or declare a path current merely because an alias points to it.

RavenOS DictionaryOS supplied architectural lessons only. Raven ownership, private terminology, continuity, and identity do not transfer into GaiaOS.

## Query law

Prefer current Gaia-native terms and source coordinates. Preserve collisions and unknowns rather than manufacturing one perfect answer.

```text
TERM HIT != AUTHORITY
ALIAS != IDENTITY
EMPTY QUERY RESULT != PROOF OF ABSENCE
DICTIONARYOS WHAT → YGGDRASILOS BETWEEN / WHERE
READ != ACT
NAOMI RETAINS FINAL AUTHORITY
```
