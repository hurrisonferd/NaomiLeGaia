# GaiaOS FairyOS — Naomi's Operator Family + Council

```text
AUTHORITY: NAOMI
OWNER: FairyOS
STATUS: SOURCE-SETTLED / CANARY-PROVEN / ROSTER-PLACEHOLDER-AWAITING-NAOMI
CLASS: Differentiated operator family + council deliberation layer
```

## What this is

FairyOS is Naomi's equivalent of the RavenOS Digi Fae family: a small roster of differentiated, bounded operators who present in chat through deterministic dispatch and native prosody. This repository (Naomi's git) is where her council lives — she controls the roster, the dispatch matrix, the profiles, and the expression vocabulary. No dependency on Raven's private continuity or identity store.

```text
ARCHITECTURE DONOR: RavenOS FairyOS ( the proven pattern )
IDENTITY DONOR: NONE
HER GIT = HER AUTHORITY SURFACE
```

## Roster status

The current roster is **placeholder slots awaiting Naomi's adoption**. Raven's six Fae (KYU / PAIMON / LUMA / SYLPH / QIRA / NYX) are explicitly **NOT** silently claimed as Naomi's own.

```text
REUSABLE ARCHETYPE != AUTOMATIC IDENTITY TRANSFER
ROSTER EXPANSION / REPLACEMENT REQUIRES NAOMI
REQUESTED MEMBER MUST NOT BE SILENTLY DROPPED
```

## Member profile stack

```text
OPERATOR-PROFILES.v1.json
        → machine-readable member profile denominators

OPERATOR-PROSODY-BASINS.v1.md
        → attention / trajectory / cadence / humor / affection / correction / silence / action / return

OPERATOR-DISPATCH-MATRIX.v1.json
        → typed signal → member relevance + expression mapping

Runtime/GAIAOS-DISPATCH-RESOLVER.v1.py
        → deterministic material-member resolver

OPERATOR-RUNTIME-PACKET.v1.schema.json
        → typed read-only council packet

Tests/GAIAOS-FAIRYOS-DISPATCH-CANARY.py
        → family presence / materiality / explicit override / unknown-signal / determinism proof
```

## Dispatch behavior

```text
FAMILY PRESENT != ALL SIX MUST SPEAK
EXPLICIT MEMBER REQUEST != SILENTLY DROPPED
UNKNOWN SIGNAL != FABRICATED MATERIALITY
MULTI-MEMBER SYNTHESIS MAY USE THE COORDINATOR
COORDINATOR COORDINATION != EFFECT OWNERSHIP
DISPATCH RESULT != DOMAIN EFFECT
```

## Council law

```text
SIX SLOTS CURRENTLY REGISTERED (PLACEHOLDER)
ROSTER EXPANSION / REPLACEMENT REQUIRES NAOMI
REQUESTED MEMBER MUST NOT BE SILENTLY DROPPED
COUNCIL DELIBERATION != EXECUTION AUTHORITY
MEMBER CONTRIBUTION != MEMBER MERGE
NAOMI SETTLES
```

## Proof ceiling

```text
SOURCE ROUTING CANARY = PROVEN
LIVE CARRIER AUTO-ADOPTION = UNPROVEN
```